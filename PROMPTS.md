# frontend/main.py:
    
    create a streamlit chatbot user interface. i have an api ready to be called that streams the chatbots response

    Use streamlits st.chat_message method to create a chatbot interface

    create a post request to my backend to receive the response in streams

    only get the ais response message no extra metadata like token usage

# hello-ai/index.ts

    i need to be able to make a post request from the frontend so i can send the input message to the worker backend.

    make a typescript type for the user prompt its saying body.prompt doenst exits